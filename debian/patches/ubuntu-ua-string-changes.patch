Description: Add "Ubuntu" to the platform part of the UA string
Author: Chris Coulson <chris.coulson@canonical.com>
Forwarded: not-needed

Index: firefox-16.0~a2~hg20120810r102348/netwerk/protocol/http/Makefile.in
===================================================================
--- firefox-16.0~a2~hg20120810r102348.orig/netwerk/protocol/http/Makefile.in	2012-08-11 06:18:45.000000000 +0100
+++ firefox-16.0~a2~hg20120810r102348/netwerk/protocol/http/Makefile.in	2012-08-12 00:08:15.480220976 +0100
@@ -98,3 +98,7 @@
 include $(topsrcdir)/config/rules.mk
 
 DEFINES += -DIMPL_NS_NET
+
+ifdef MOZ_UA_VENDOR
+DEFINES += -DMOZ_UA_VENDOR=\""$(MOZ_UA_VENDOR)"\"
+endif
Index: firefox-16.0~a2~hg20120810r102348/netwerk/protocol/http/nsHttpHandler.cpp
===================================================================
--- firefox-16.0~a2~hg20120810r102348.orig/netwerk/protocol/http/nsHttpHandler.cpp	2012-08-11 06:18:45.000000000 +0100
+++ firefox-16.0~a2~hg20120810r102348/netwerk/protocol/http/nsHttpHandler.cpp	2012-08-12 00:15:28.568238767 +0100
@@ -160,6 +160,9 @@
     , mLegacyAppName("Mozilla")
     , mLegacyAppVersion("5.0")
     , mProduct("Gecko")
+#ifdef MOZ_UA_VENDOR
+    , mVendor(MOZ_UA_VENDOR)
+#endif
     , mUserAgentIsDirty(true)
     , mUseCache(true)
     , mPromptTempRedirect(true)
@@ -291,6 +294,9 @@
     LOG(("> misc = %s\n", mMisc.get()));
     LOG(("> product = %s\n", mProduct.get()));
     LOG(("> product-sub = %s\n", mProductSub.get()));
+#ifdef MOZ_UA_VENDOR
+    LOG(("> vendor = %s\n", mVendor.get()));
+#endif
     LOG(("> app-name = %s\n", mAppName.get()));
     LOG(("> app-version = %s\n", mAppVersion.get()));
     LOG(("> compat-firefox = %s\n", mCompatFirefox.get()));
@@ -536,6 +542,9 @@
     // than if we didn't preallocate at all.
     mUserAgent.SetCapacity(mLegacyAppName.Length() + 
                            mLegacyAppVersion.Length() + 
+#ifdef MOZ_UA_VENDOR
+                           mVendor.Length() + 
+#endif
 #ifndef UA_SPARE_PLATFORM
                            mPlatform.Length() + 
 #endif
@@ -547,7 +556,7 @@
                            mAppVersion.Length() +
                            mCompatFirefox.Length() +
                            mCompatDevice.Length() +
-                           13);
+                           15);
 
     // Application portion
     mUserAgent.Assign(mLegacyAppName);
@@ -567,6 +576,10 @@
         mUserAgent.AppendLiteral("; ");
     }
 #else
+#ifdef MOZ_UA_VENDOR
+    mUserAgent += mVendor;
+    mUserAgent.AppendLiteral("; ");
+#endif
     mUserAgent += mOscpu;
     mUserAgent.AppendLiteral("; ");
 #endif
Index: firefox-16.0~a2~hg20120810r102348/netwerk/protocol/http/nsHttpHandler.h
===================================================================
--- firefox-16.0~a2~hg20120810r102348.orig/netwerk/protocol/http/nsHttpHandler.h	2012-08-11 06:18:45.000000000 +0100
+++ firefox-16.0~a2~hg20120810r102348/netwerk/protocol/http/nsHttpHandler.h	2012-08-12 00:08:15.480220976 +0100
@@ -324,6 +324,7 @@
     // useragent components
     nsCString      mLegacyAppName;
     nsCString      mLegacyAppVersion;
+    nsCString      mVendor;
     nsCString      mPlatform;
     nsCString      mOscpu;
     nsCString      mMisc;
